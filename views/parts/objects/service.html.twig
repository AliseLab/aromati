<div class="col-md-4 {{ cls }}" data-id="{{ id }}">
	<div class="thumbnail">
		<i class="material-icons">trending_up</i>
		<h4 class="service__title">{{ trans( req, uid ~ '_header') }}</h4>
		<p class="service__paragraph">{{ trans( req, uid ~ '_text') }}</p>
	</div>
</div>
